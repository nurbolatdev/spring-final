spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: false

  sql:
    init:
      mode: never

jwt:
  secret: "test-secret-key-for-jwt-testing-purposes-only-32chars"
  expiration: 86400000

file:
  upload-dir: /tmp/test-uploads/

logging:
  level:
    root: WARN
    com.example.finalproject: ERROR
